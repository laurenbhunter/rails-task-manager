<p><%= @task.title %></p>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>

<%= link_to "Edit a task", edit_task_path%>

<%= link_to "Delete",
  delete_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
